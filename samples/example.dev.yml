# adding `mode: dev` will install to require-dev instead of require

spec: 0.1
install:
  package: barryvdh/laravel-debugbar:v3.2.4
  mode: dev
meta:
  name: flavour/laravel-debugbar
  version: v3.2.4
